apiVersion: v1
kind: Service
metadata:
  name: auth
  namespace: default
  labels:
    app: auth
spec:
  type: ClusterIP
  ports:
    - name: grpc
      port: 9100
      targetPort: 9100
    - name: http
      port: 8100
      targetPort: 8100
  selector:
    app: auth
